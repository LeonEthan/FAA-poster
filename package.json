{
  "name": "faa-poster",
  "version": "0.1.0",
  "description": "PosterStack — AI poster generation with gstack + evolver",
  "main": "bridge/index.js",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "setup-skills": "bash bin/setup-skills"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.4.0",
    "fs-extra": "^11.2.0",
    "glob": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
